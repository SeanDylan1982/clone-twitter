specVersion: "v1"
serviceId: "clone"
location: "northamerica-northeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "clone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
